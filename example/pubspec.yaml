name: example
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.10.0 <3.0.0"

publish_to: none

dependencies:
  flutter:
    sdk: flutter

  nuvigator:
    path: ../
  provider: ^6.0.3

dev_dependencies:
  build_runner: ^2.0.3
  flutter_test:
    sdk: flutter
  pedantic: ^1.11.0

flutter:
  uses-material-design: true
