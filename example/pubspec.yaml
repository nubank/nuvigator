name: example
description: A new Flutter project.

version: 1.0.0+2

environment:
  sdk: ">=2.17.0 <4.0.0"

publish_to: none

dependencies:
  flutter:
    sdk: flutter

  nuvigator:
    path: ../
  provider: ^6.0.3

dev_dependencies:
  build_runner: ^2.0.3
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
